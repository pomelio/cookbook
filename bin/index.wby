import ext.HttpContext as web;
import std.string as str;


let ppath = web.path();

if ppath == '/' {
  ppath = 'docs/start.md';
}

if str.ends_with(ppath, '.md') {
  web.dispatch('/markdown', {});
} else {
  web.dispatch(ppath, {});
}
